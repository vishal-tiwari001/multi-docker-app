version: 2.1

jobs:
  client_testing:
    docker:
      - image: cimg/base:2022.03
    steps:
      - checkout
      - setup_remote_docker:
          version: '20.10.11'
      - run:
          command: docker build -t vishalt/multi-client-2 ./clienttypescript/Dockerfile.dev ./clienttypescript
          name: building the production docker image
      - run:
          command: docker run vishalt/multi-worker-2 ./
          name: building the production docker image
      - run:
          command: docker run vishalt/client-test npm test
          name: building the production docker image
      - run:
          command: docker run vishalt/client-test npm test
          name: building the production docker image
workflows:
  version: 2
  testing:
    jobs:
      - client_testing
      - build_and_push
